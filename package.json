{
  "name": "puppeteer",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "test": "[ ! -e node_modules ] && yarn install; jest"
  },
  "dependencies": {
    "jest": "^26.1.0",
    "jest-puppeteer": "^4.4.0",
    "puppeteer": "^10.0.0"
  },
  "devDependencies": {
    "@types/expect-puppeteer": "^4.4.3",
    "@types/jest-environment-puppeteer": "^4.3.2",
    "@types/puppeteer": "^3.0.1"
  }
}
